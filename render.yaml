services:
  - type: web
    name: macrocutie
    env: node
    plan: free
    buildCommand: npm install --production --no-optional --no-audit --no-fund
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
